import { PublishTipForm } from '../components/forms/PublishForm';
import { Layout } from '../components/Layout';

export const PublishPage = () => {
    return (
        <Layout>
            <PublishTipForm />
        </Layout>
    );
};
